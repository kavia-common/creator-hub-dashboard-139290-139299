# creator-hub-dashboard-139290-139299

This workspace contains the dashboard_frontend React application for the Creator Hub project.
- Navigate to dashboard_frontend and run `npm install` then `npm start`.
- Configure OAuth via `.env` as described in `dashboard_frontend/README.md`.